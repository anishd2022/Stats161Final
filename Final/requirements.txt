# Core web framework
Flask==3.1.2
flask-cors==6.0.1
gunicorn==21.2.0

# Google Gemini API
google-generativeai==0.8.5

# Database
PyMySQL==1.1.2

# Data processing
pandas==2.3.3
# numpy will be installed automatically as a pandas dependency
# Explicitly specify to ensure we get a version with pre-built wheels
numpy>=1.24.0,<2.0.0

# Environment variables
python-dotenv==1.2.1

# RAG system
chromadb==1.3.4
sentence-transformers==5.1.2

# Note: scipy is NOT included here because:
# 1. It requires Fortran compiler to build from source
# 2. It's optional - synthetic_generator.py handles its absence gracefully
# 3. Statistical validation works without it (just less detailed)
# If you need scipy for analyze_data_statistics.py, install it locally only
